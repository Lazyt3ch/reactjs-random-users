const getColumnWidths = results => {
  let columnWidths = new Array(results.length).fill(0);
  // for (const rowObj of results) {
  //   for (const part of Object.values(rowObj)) {

  //   }
  // }

  return columnWidths;
}

export default getColumnWidths;